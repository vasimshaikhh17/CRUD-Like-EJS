<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
        <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
            <div class="w-full flex justify-end">
                <a href="/logout" class="bg-red-500 text-sm rounded-md px-3 py-2 mb-5 inline-block">Logout</a>
            </div>
            <h3 class="text-3xl mb-5"><span class="font-light">Edit Your PostPost ✏️</h3>
            <form action="/update/<%=post._id%>" method="post">
                <textarea placeholder="What's on Your mind ?" class="resize-none p-3 outline-none w-1/3 bg-transparent border-2 border-zinc-800 rounded-md" name="content" id=""><%= post.content%></textarea>
                <input type="submit" class="px-3 text-sm w-40 py-2 bg-orange-500 text-black block rounded-md mt-2" value="Create New Post" >
            </form>
           
        </div>
</body>
</html>